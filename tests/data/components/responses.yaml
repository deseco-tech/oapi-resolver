CreateResponse:
  content:
    application/json:
      schema:
        properties:
          data:
            type: object
            properties:
              retailer:
                $ref: '#/components/schemas/Item'
